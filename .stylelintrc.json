{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind"]
      }
    ],
    "property-no-unknown": [
      true,
      {
        "ignoreProperties": ["scrollbar-width", "scrollbar-gutter"]
      }
    ]
  }
}
